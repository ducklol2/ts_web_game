{
  "name": "ts_web_game",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "tsc --build client && tsc --build server && cp client/*.html client/dist/ && cp client/*.css client/dist/",
    "start": "node server/dist/server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^5.5.4"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
